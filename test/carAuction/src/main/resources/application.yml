
# mysql
spring:
  datasource:
    #MySQL配置
    driverClassName:  com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/auction?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 12263816

mybatis:
  mapper-locations: classpath:mapper/**.xml
  type-aliases-package: com.example.demo.model
  configuration:
    call-setters-on-nulls: true

server:
  port: 9000